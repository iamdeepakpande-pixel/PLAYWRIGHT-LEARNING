// bitwise & logic operator
// The bitwise AND operator (&) compares each bit of two numbers and returns 1 if both bits are 1, otherwise it returns 0.
// AND operator -  &  -  compares each bit of two numbers and returns 1 if both bits are 1, otherwise it returns 0.
// OR operator -  |  -  compares each bit of two numbers and returns 1 if either bit is 1, otherwise it returns 0.

// AND &

// let a1 = 5; // 0101
// let a2 = 3; // 0011

// console.log(a1 & a2); // 1 (0001) - only the last bit is 1 in both numbers
// console.log(a1 | a2); // 7 (0111) - the first three bits are 1 in either number

// 5= 0101
//3= 0011

// OR |
