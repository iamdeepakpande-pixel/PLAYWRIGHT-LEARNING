// js brings var , let, const , function declarations to the top of their scope before code execution. This is called hoisting.
// implicit global variable cannot be  hoisted to the top of the global scope
